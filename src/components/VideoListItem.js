import React from 'react';

const VideoListItem = ({video}) => {
  // const video = props.video; not needed when using {video} inside of callback argument
  
  return <li>Video</li>;
};

export default VideoListItem;
